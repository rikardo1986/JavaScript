// Modulos externos, se debe renombrar el archivo con la extencion .cjs

const os = require("os");

console.log(os.platform());
console.log(os.arch());
console.log(os.totalmem());
console.log(os.cpus());
console.log(os.hostname());
